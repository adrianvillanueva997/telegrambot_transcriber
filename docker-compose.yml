services:
  bot:
    image: ghcr.io/adrianvillanueva997/telegrambot_transcriber:v2.1.0
    container_name: telegrambot_transcriber
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
